Render an HTML list. The `for` and `end` tags should each occupy their
own line in the source but disappear entirely from the output, while the
two-space indentation of each `<li>` should be preserved. Use the
non-greedy whitespace control (`~`).
