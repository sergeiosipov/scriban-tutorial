The data model provides three loose word fields — `first`, `second`,
`third` — that you should collect into an array `words`, then print
them in reverse order separated by colons.

Steps:

1. Start with `words = []`.
2. Append `first`, `second`, `third` to `words` by assigning to indices
   `0`, `1`, `2`.
3. Print `words[2] : words[1] : words[0]` (i.e. reversed) with `:`
   separators (no spaces).

With the data below the output should read `gamma:beta:alpha`.
