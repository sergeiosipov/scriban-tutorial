The data model carries an `items` array of three colours. Print the
**first** item, then a hyphen wrapped in spaces, then the **third** item.

Reminder: array indices in Scriban start at **0**, not 1 — so the first
element is `items[0]` and the third is `items[2]`.
