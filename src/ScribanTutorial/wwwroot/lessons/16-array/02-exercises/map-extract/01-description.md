Pluck a single field from each element in an array of objects. The
data has a `products` array of `{name, price}` records — extract the
`name` field of each with `array.map`, then join the names with `" | "`.

With the data below the expected output is `Apple | Banana | Cherry`.
