Filter an array down to positive values only. Define a small simple
function `is_positive` that returns `true` when `$0 > 0`, then use
`@is_positive` with `array.filter`.

With `xs = [3, -1, 4, -5, 9, -2, 6]` the expected output is `[3, 4, 9, 6]`.
