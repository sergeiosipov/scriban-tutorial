Find the **three smallest** values from `numbers`. Pipe through
`array.sort` to order ascending, then `array.limit 3` to keep the top
three. Print the result as `array.join ", "`.

With `numbers = [42, 7, 18, 3, 25, 11]` the expected output is `3, 7, 11`.
