Deduplicate a tag list, sort it alphabetically, and join with commas.

With `tags = ["scriban", "csharp", "templates", "csharp", "blazor", "scriban"]`
the expected output is `blazor, csharp, scriban, templates`.
