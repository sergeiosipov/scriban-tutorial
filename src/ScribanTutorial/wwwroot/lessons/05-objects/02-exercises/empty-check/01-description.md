The data model carries two objects: `a` is `{}` and `b` is `{ x: 1 }`.
Print whether each is empty using the `.empty?` property. The expected
output is `true` on the first line and `false` on the second.
