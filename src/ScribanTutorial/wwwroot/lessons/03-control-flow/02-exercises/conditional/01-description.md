### Conditional

Print the stock status of a product. Expected output (for this data):

```text
widget — in stock (5)
```

If `stock` is zero, the same template must say `out of stock` instead. Use
`if`/`else` with the right comparison.
