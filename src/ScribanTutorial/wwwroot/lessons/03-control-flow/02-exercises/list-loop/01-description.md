### List loop

Render the items as a bulleted list, one per line. Expected output:

```text
- apple
- banana
- cherry
```

The starter sketches the shape; you need to fill in the `for` header and the
body so it visits each item.
