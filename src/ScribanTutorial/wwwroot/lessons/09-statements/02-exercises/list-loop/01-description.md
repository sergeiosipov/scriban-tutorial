Render the `items` array as a Markdown bulleted list. Each item should
appear on its own line as `- <item>`. Use `for ... in ... end` and trim
the block tags so there are no blank lines between items.
