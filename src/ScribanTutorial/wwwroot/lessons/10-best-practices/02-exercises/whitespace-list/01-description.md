Render a clean bulleted list with no blank lines between items. The
template below has the structure but not the whitespace-control dashes —
add them to the `for` and `end` tags so the output is exactly:

```
- alpha
- beta
- gamma
```
