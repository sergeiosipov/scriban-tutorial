The template prints `name`, but the line is cluttered with a remark you
don't want in the output. Add a single-line comment marker so the rest of
the line is ignored — but `name` still prints. The data model supplies
`name = "Ada"`.
