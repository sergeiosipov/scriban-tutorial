Given the float `n`, produce three lines:

    up   <ceil>
    down <floor>
    near <round>

Where each `<...>` is `n` rounded the corresponding direction.

With `n = 3.7` the expected output is:

    up   4
    down 3
    near 4
