{{- func longest
     winner = ""
     for s in ???
       if s.size > winner.size; winner = s; end
     end
     ret winner
   end -}}
{{ pick = longest a b c d
   "pick: " + ??? + ", length: " + ???.size }}
