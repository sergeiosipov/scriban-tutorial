Parse two numeric strings and compute their average.

The data has `a_str = "42"` and `b_str = "58"` — both stored as
strings, as if they came from form input or a CSV column. Convert
both with `string.to_int`, sum them, divide by `2`, and print the
result.

Expected output: `50`.
