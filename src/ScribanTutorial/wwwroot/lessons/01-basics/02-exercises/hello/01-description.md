### Hello

Render a greeting using the `name` value from the data model. The expected
output is one line:

```text
Hello, Ada!
```

Replace the `???` placeholder in the starter template with the right expression.
