### Member access

Render the user's full name from the nested `user` object. Expected output is
exactly one line:

```text
Ada Lovelace
```

Fill in the two `???` placeholders so the template reaches into `user` for both
name parts.
