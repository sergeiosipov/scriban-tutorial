@* Renders pre-built HTML. Stage 6 starts shipping pre-highlighted code spans. *@

<section class="theory">
    <div class="theory__badge">Theory</div>
    <div class="theory__body">
        @* Source is ContentBuilder-rendered Markdown from the repo. If you ever
           load HTML from an untrusted source, sanitise first. *@
        @((MarkupString)Html)
    </div>
</section>

@code {
    [Parameter, EditorRequired] public string Html { get; set; } = "";
}
